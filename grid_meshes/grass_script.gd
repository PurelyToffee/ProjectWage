extends Node3D


func _process(delta: float) -> void:
	#var target = LevelController.player.global_position
	#target.y = global_position.y  # flatten to same height
	#look_at(target, Vector3.UP)
	pass;
